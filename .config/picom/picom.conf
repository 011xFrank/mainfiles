backend = "glx";

active-opacity = 1;
inactive-opacity = 1;
frame-opacity = 1;

opacity-rule =[
        "90:class_g = 'Alacritty' && focused",
        "70:class_g = 'Alacritty' && !focused"
]

blur-background = true;
blur-method = "dual_kawase";
